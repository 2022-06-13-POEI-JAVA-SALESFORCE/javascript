// boucle for cf demo array.js

let nb = 1
for (let i = 0; i < 10; i++) {
    // Traitements
}

// boucle while cf demo sur array.js
while (// condition) { // Peut ne pas s'execute dans votre prog si la condition n'est pas rempli
    // Traitements
}

do { // s'execute au moins une fois peu importe la condition (remplie ou non)
    // instructions s'execute au moins une fois
} while (// condition) // condition evalué après